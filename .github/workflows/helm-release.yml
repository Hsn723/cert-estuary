name: Release Helm chart
on:
  push:
    branches:
      - master
    paths:
      - 'charts/cert-estuary/Chart.yaml'
permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
jobs:
  release:
    uses: hsn723/actions/.github/workflows/helm-release.yml@d7fb9a279f99f27dfd9aa008a97c6ec92ccfa330 # v1.0.34
    secrets: inherit
    with:
      upload_as_oci: true
      chart_name: cert-estuary
